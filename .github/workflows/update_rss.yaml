name: "update rss"

on:
  schedule:
    # https://crontab.guru/#0_0_*_*_*
    # At 00:00 (UTC)
    - cron: "0 0 * * *"

  workflow_dispatch: # allow manually running
    inputs:
      log_level:
        type: choice
        options:
          - NOTSET
          - DEBUG
          - INFO
          - WARNING
          - ERROR
        default: INFO
      publish:
        type: boolean
        default: false

permissions: {}

# ensure at most one publish job runs at a time
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

env:
  FORCE_COLOR: 1

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: true

      - name: Restore pointer HTML cache
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: .cache/pointer
          key: pointer-html-v1-${{ runner.os }}
          restore-keys: |
            pointer-html-v1-

      - name: build rss feed
        env:
          LOG_LEVEL: ${{ inputs.log_level }}
        run: |
          mkdir _site
          uv run pointer-io-rssfeed > _site/feed.xml

      - name: Upload static files as artifact
        if: ${{ github.event_name == 'schedule' || inputs.publish == true }}
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        id: deployment
        with:
          path: _site/

  deploy:
    # deploy if running on schedule or the publish flag is set
    if: ${{ github.event_name == 'schedule' || inputs.publish == true }}
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write # to deploy to Pages
      id-token: write # to verify the deployment originates from an appropriate source
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4.0.5
        id: deployment
